# 2026-03-13

- Added SSH access profile for `msk1` (`80.249.151.50`) as `root` using `~/.ssh/msk1_ed25519`; connection test passed (`hostname=forsaken-cavallo`, user=root).
- Documented local operator note: `3x-ui` is installed on `msk1`.
- For `sntchayka.prosnt.ru`, direct host/wg requests were blocked by SmartCaptcha; successful extraction required routing through `phone-proxy` (SOCKS5 on `127.0.0.1:1081` via Anton’s Termux tunnel).
- Extracted electricity dataset from site pages/PDFs: downloaded 56 electricity PDFs into `~/dev/sntchayka/electricity_pdfs` and parsed into PostgreSQL tables `sntchayka.electricity_files` + `sntchayka.electricity_readings`.
- Fixed parser quality issue: many PDFs split numeric columns across the next line; parser was updated to merge continuation lines and recover missing months/rows (notably for plot 346), then DB was fully reloaded with deduplication by `(plot_no, month_date)`.
- Built/updated web-adam electricity view at `/snt-electricity` (filters by year/month/plot regex, exports CSV/XLSX, heatmap table, top rise/fall blocks) and fixed theme picker behavior for that page.
- Installed Grafana 12.4.1 on this host for SNT analytics: admin `tony` / `tony1pwd`, HTTP port `33917`, access restricted in UFW to 4 whitelisted VPS IPs (`80.66.87.163`, `80.249.151.50`, `150.241.122.203`, `185.28.175.67`).
- Created Grafana datasource `SNT Postgres Live` and dashboard `SNT Chayka Electricity` (`/d/sntchayka-electricity/snt-chayka-electricity`), then iteratively fixed datasource/panel compatibility issues (mobile “No data”), added plot multi-select variable (`All` + search), per-plot timeseries, and summed timeseries for selected plots.
